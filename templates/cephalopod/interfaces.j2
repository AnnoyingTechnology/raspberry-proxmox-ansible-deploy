auto lo
iface lo inet loopback

iface eth0 inet manual

auto vmbr0
iface vmbr0 inet static
    address {{ ansible_host }}/24
    gateway {{ gateway }}
    bridge-ports eth0
    bridge-stp off
    bridge-fd 0

